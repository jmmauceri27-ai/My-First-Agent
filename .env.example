# Postgres connection string (Vercel Postgres, Supabase, Neon, etc.)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Optional: set a passcode to gate the whole site behind a simple login page.
# Leave unset / empty during local dev to skip the login screen entirely.
SITE_PASSWORD=""
