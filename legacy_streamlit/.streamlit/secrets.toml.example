# Copy this file to .streamlit/secrets.toml (locally) and fill in a real password.
# Never commit the real secrets.toml — it's gitignored.
#
# When deployed on Streamlit Community Cloud, set this same key instead in
# your app's Settings -> Secrets, rather than committing a secrets.toml file.

app_password = "change-me"
